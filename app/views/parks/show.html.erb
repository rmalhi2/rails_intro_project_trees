<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Park Details</title>
  <style>
    .custom-link {
      color: black;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Park Details</h1>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/trees">Trees</a></li>
            <li class="breadcrumb-item"><a href="<%= tree_path(@tree) %>"><%= @tree.common_name %></a></li>
            <li class="breadcrumb-item active" aria-current="page"><%= @park.park_name %></li>
        </ol>
    </nav>

    <p><strong>Park Name:</strong> <%= @park.park_name %></p>
    <p><strong>Trees:</strong></p>
    <ul>
      <% @park.trees.each do |tree| %>
        <li><%= link_to tree.common_name, tree_path(tree), class: "custom-link" %></li>
      <% end %>
    </ul>

    <%= link_to "Back", :back , class: "btn btn-primary" %>
  </div>
</body>
</html>
